# Pendahuluan

Selamat datang di kursus Hugging Face! Pengantar ini akan memandu Anda dalam menyiapkan lingkungan kerja. Jika Anda baru memulai kursus, kami sarankan untuk terlebih dahulu melihat [Bab 1](/course/chapter1), kemudian kembali ke sini untuk menyiapkan lingkungan agar Anda dapat mencoba kode sendiri.

Semua pustaka yang akan kami gunakan dalam kursus ini tersedia sebagai paket Python. Di sini kami akan menunjukkan cara menyiapkan lingkungan Python dan menginstal pustaka spesifik yang Anda perlukan.

Kami akan membahas dua cara menyiapkan lingkungan kerja: menggunakan notebook Colab atau lingkungan virtual Python. Silakan pilih metode yang paling sesuai. Untuk pemula, kami sangat menyarankan memulai dengan notebook Colab.

Perhatikan bahwa kami tidak akan membahas sistem Windows. Jika Anda menggunakan Windows, kami sarankan mengikuti kursus dengan notebook Colab. Jika Anda menggunakan distribusi Linux atau macOS, Anda bisa menggunakan salah satu pendekatan yang dijelaskan di sini.

Sebagian besar kursus mengharuskan Anda memiliki akun Hugging Face. Kami sarankan membuatnya sekarang: [buat akun](https://huggingface.co/join).

## Menggunakan Google Colab

Menggunakan notebook Colab adalah penyiapan paling sederhana; luncurkan notebook di browser Anda dan langsung mulai membuat kode!

Jika Anda belum familiar dengan Colab, kami sarankan memulai dengan mengikuti [pengantar](https://colab.research.google.com/notebooks/intro.ipynb). Colab memungkinkan penggunaan perangkat akselerasi seperti GPU atau TPU, dan gratis untuk beban kerja kecil.

Setelah Anda nyaman menggunakan Colab, buat notebook baru dan mulai penyiapan:

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter0/new_colab.png" alt="Sebuah notebook colab kosong" width="80%"/>
</div>

Langkah selanjutnya adalah menginstal pustaka yang akan digunakan dalam kursus. Kami akan menggunakan `pip` untuk instalasi, yaitu manajer paket Python. Di notebook, Anda bisa menjalankan perintah sistem dengan menambahkan karakter `!`, sehingga Anda bisa menginstal pustaka 🤗 Transformers seperti ini:

```
!pip install transformers
```

Pastikan paket terinstal dengan benar dengan mengimpornya di runtime Python:

```
import transformers
```

<div class="flex justify-center">
<img src="https://huggingface.co/datasets/huggingface-course/documentation-images/resolve/main/en/chapter0/install.gif" alt="Gif menunjukkan hasil dua perintah di atas: instalasi dan impor" width="80%"/>
</div>

Ini menginstal versi 🤗 Transformers yang sangat ringkas. Secara khusus, *tidak ada* kerangka kerja machine learning spesifik (seperti PyTorch atau TensorFlow) yang terinstal. Karena kami akan menggunakan banyak fitur berbeda dari pustaka ini, kami sarankan menginstal versi pengembangan yang dilengkapi semua dependensi yang diperlukan untuk hampir semua kasus penggunaan:

```
!pip install transformers[sentencepiece]
```

Proses ini membutuhkan waktu, tetapi Anda akan siap untuk melanjutkan sisa kursus!

## Menggunakan Lingkungan Virtual Python

Jika Anda lebih suka menggunakan lingkungan virtual Python, langkah pertama adalah menginstal Python di sistem Anda. Kami sarankan mengikuti [panduan ini](https://realpython.com/installing-python/).

Setelah Python terinstal, Anda harus bisa menjalankan perintah Python di terminal. Mulailah dengan menjalankan perintah berikut untuk memastikan instalasi benar sebelum melanjutkan: `python --version`. Ini akan menampilkan versi Python yang tersedia di sistem Anda.

Saat menjalankan perintah Python di terminal (seperti `python --version`), program yang menjalankan perintah Anda adalah Python "utama" di sistem. Kami sarankan menjaga instalasi utama ini bebas dari paket apa pun, dan menggunakannya untuk membuat lingkungan terpisah untuk setiap aplikasi—dengan cara ini, setiap aplikasi memiliki dependensi dan paketnya sendiri tanpa risiko masalah kompatibilitas.

Di Python, ini dilakukan dengan [*lingkungan virtual*](https://docs.python.org/3/tutorial/venv.html), yaitu direktori mandiri yang berisi instalasi Python dengan versi spesifik beserta semua paket yang diperlukan aplikasi. Anda bisa membuatnya dengan berbagai alat, tapi kami akan menggunakan paket resmi Python bernama [`venv`](https://docs.python.org/3/library/venv.html#module-venv).

Pertama, buat direktori untuk aplikasi Anda—misalnya, direktori baru bernama *transformers-course* di root direktori home:

```
mkdir ~/transformers-course
cd ~/transformers-course
```

Dari dalam direktori ini, buat lingkungan virtual menggunakan modul `venv` Python:

```
python -m venv .env
```

Sekarang seharusnya ada direktori bernama *.env* di folder yang sebelumnya kosong:

```
ls -a
```

```out
.      ..    .env
```

Anda bisa masuk/keluar lingkungan virtual dengan skrip `activate` dan `deactivate`:

```
# Aktifkan lingkungan virtual
source .env/bin/activate

# Nonaktifkan lingkungan virtual
deactivate
```

Pastikan lingkungan aktif dengan perintah `which python`: jika mengarah ke lingkungan virtual, Anda berhasil mengaktifkannya!

```
which python
```

```out
/home/<user>/transformers-course/.env/bin/python
```

### Menginstal Dependensi

Seperti di bagian sebelumnya tentang Google Colab, Anda perlu menginstal paket yang diperlukan. Sekali lagi, gunakan manajer paket `pip` untuk menginstal versi pengembangan 🤗 Transformers:

```
pip install "transformers[sentencepiece]"
```

Sekarang Anda siap melanjutkan!
